<catel:DataWindow x:Class="EmulationProcessing.Views.ProviderView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:catel="http://schemas.catelproject.com">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding ProviderName}" />
        <Button Grid.Row="1" 
                Width="150"
                Command="{Binding OpenCommand}">Выбрать изображение</Button>
        <Image Grid.Row="2" Width="50" Source="{Binding ProviderLogo}" />
    </Grid>
    
</catel:DataWindow>
